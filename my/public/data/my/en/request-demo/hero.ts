import { HeroProps } from '@/sections/request-demo/Hero';
import { textImageData } from './text-image';
import { formMarqueeData } from './formMarquee';

export const heroData: HeroProps = {
  textImageData: textImageData,
  formMarqueeData: formMarqueeData,
};
