import { FormMarqueeProps } from '@/components/request-demo';
import { logoContainerData } from './LogoContainer';
import { formData } from './form';

export const formMarqueeData: FormMarqueeProps = {
  formData: formData,
  logoContainerData: logoContainerData,
};
